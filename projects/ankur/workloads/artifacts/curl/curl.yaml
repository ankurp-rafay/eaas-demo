apiVersion: v1
kind: Pod
metadata:
  name: curl-test
spec:
  containers:
  - name: curl
    image: curlimages/curl:latest # Use the official curl container image
    command: ["sleep", "infinity"] # Keep the container running indefinitely
